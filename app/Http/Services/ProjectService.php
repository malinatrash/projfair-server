<?php

namespace App\Http\Services;

use App\Models\Candidate;
use App\Models\Project;

class ProjectService
{

    // function filterByUserSpeciality($projects, $token)
    // {
    //     $candidates = Candidate::where('api_token', $token);
    //     if (count($candidates) == 0) {
    //         return $projects;
    //     }
    //     $candidate = $candidates[0];


    //     $candidateSpeciality = explode("-", $candidate['speciality'])[0];
    // }
}
